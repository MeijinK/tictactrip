export function compterMots(texte: string): number {
  return 0;
}

export function verifierLimite(token: string, nombreMots: number): boolean {
  return false;
}

export function incrementerCompteur(token: string, nombreMots: number): void {
  // TODO
}
